# joycaption-2api-cfwork
逆向工程Gradio API、无头浏览器(Headless Browser)模拟、无需Cookie自动持久化会话、高级匿名伪装(User-Agent轮换)、自动绕过Cloudflare人机验证、原生流式SSE转换、多模型兼容映射(GPT-4V/GPT-4o)、Base64图片处理、ArrayBuffer二进制操作、CORS跨域支持、错误处理与状态监控、Web UI集成测试
